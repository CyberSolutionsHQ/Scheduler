/* assets/config.example.js
   Copy to assets/config.js for local overrides (kept out of git by default).
   This file is safe to commit (no secrets).
*/

// eslint-disable-next-line no-unused-vars
window.APP_CONFIG = {
  USE_CLOUD: false,

  // Supabase (placeholder only; do not add keys here in git)
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",

  // Future support mode stub (do not implement yet)
  SUPPORT_MODE: false,
};

